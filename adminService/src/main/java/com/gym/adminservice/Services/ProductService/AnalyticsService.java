package com.gym.adminservice.Services.ProductService;

import org.springframework.stereotype.Service;

@Service
public class AnalyticsService {
    /*
     * as of now we don't need any analytics service methods
     * because we are not building the product service now so actually we will get
     * all the
     * analytics data from the product service
     * so keeping this class for future use
     */

    /*
     * it will have methods to get analytics data like
     * total sales, total orders, total customers, total products
     * sales by category, sales by brand, sales by product
     * monthly sales, yearly sales
     * top-selling products, top customers and also which product is not selling
     * and other analytics data
     */
}
