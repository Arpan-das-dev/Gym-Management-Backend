package com.gym.adminservice.Services.PlanServices;

import org.springframework.stereotype.Service;

@Service
/*
 * this service class is responsible for managing plans by admin
 * like creating, updating, deleting and getting plans
 * as of now we are just creating the methods but not calling the webclient to call the plan service
 * because we are not building the plan service now so actually we will get all the
 */
public class PlanManagementService {

}
