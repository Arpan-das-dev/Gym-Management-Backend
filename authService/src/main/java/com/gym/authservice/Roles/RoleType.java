package com.gym.authservice.Roles;
/*
 * this enum represents the different roles available in the gym management system.
 * Each role defines a specific set of permissions and access levels for users within the system.
 */
public enum RoleType {
    ADMIN,
    MEMBER,
    TRAINER,
    TRAINER_PENDING,
    TRAINER_ADMIN, ADMIN_ADMIN
}
